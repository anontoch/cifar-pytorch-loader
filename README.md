# cifar-pytorch-loader

PyTorch / TorchVision CIFAR-10 data loader example.

| File | Purpose |
|---|---|
| `cifar_data_loader.py` | Downloads CIFAR-10 to `./data` and builds DataLoaders |

```powershell
pip install torch torchvision
python cifar_data_loader.py
```
